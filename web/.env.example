# `npm run init` copies this to .env.local (gitignored). Fill in the real values there.
# .env.local is loaded in every mode, so one copy covers both `npm run dev` and `npm run local`.
VITE_FIREBASE_DATA_NAMESPACE=production
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key
